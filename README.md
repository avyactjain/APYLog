# APYLog

Snapshots PST smart-vault positions and tracks Net APY against a 2% guarantee.

## Setup

```bash
npm install
npm run build
npm start
```

Edit `config.json` to set the RPC URL, snapshot interval, and the vault / NFT ids to watch.
